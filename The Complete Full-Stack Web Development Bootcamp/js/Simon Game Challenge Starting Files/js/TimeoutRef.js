/**
 * Class for passing ref for timeout.
 */
export class TimeoutRef {
    /**
    * @param {Number} timeout - Number indicating time between beeps.
    */
    constructor(timeout) {
        this.timeout = timeout;
    }
}